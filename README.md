# OntologyGenerGraph

OntologyGenerGraph — исследовательская система автоматической генерации графов знаний из текстовых данных с использованием больших языковых моделей (LLM) и онтологических ограничений.

Проект разработан в рамках исследования влияния онтологий на качество построения графов знаний. Система позволяет сравнивать различные стратегии извлечения знаний из текста и оценивать вклад онтологии в повышение качества получаемого графа.

## Цель проекта

Основной целью проекта является исследование методов ontology-guided knowledge graph generation и оценка влияния онтологических ограничений на качество автоматически генерируемых графов знаний.

В рамках проекта реализовано сравнение нескольких подходов:

* генерация графа знаний без использования онтологии;
* генерация графа знаний с использованием списка допустимых отношений;
* генерация графа знаний с использованием ограничений domain/range;
* генерация графа знаний с последующей онтологической валидацией.

## Используемый набор данных

Для проведения экспериментов используется набор данных **Text2KGBench (DBpedia-WebNLG)**.

Набор данных содержит:

* текстовые описания объектов предметной области;
* онтологии предметных областей;
* эталонные графы знаний в виде триплетов;
* тестовые и обучающие выборки.

В текущей реализации используется подмножество **University Ontology**.

## Архитектура системы

Система включает следующие основные компоненты:

* модуль загрузки данных;
* модуль взаимодействия с языковой моделью;
* модуль извлечения триплетов;
* модуль онтологической валидации;
* модуль расчёта метрик качества;
* модуль проведения экспериментов;
* модуль сохранения результатов.

В процессе работы текстовое описание передаётся языковой модели, которая извлекает триплеты графа знаний. Затем результаты могут быть проверены на соответствие онтологии, после чего рассчитываются метрики качества и сохраняются результаты эксперимента.

## Используемые технологии

* Python 3.11
* OpenAI SDK
* Cloud.ru Foundation Models
* GPT-4.1-mini
* Docker
* JSON
* CSV

## Методы оценки качества

Для оценки качества генерации графов знаний используются следующие метрики:

### Strict Metrics

* Precision
* Recall
* F1-score

Данные метрики требуют полного совпадения триплета с эталонным графом знаний.

### Fact Metrics

* Fact Precision
* Fact Recall
* Fact F1

Метрики оценивают качество извлечения фактов независимо от точного названия отношения.

### Ontology Metrics

* Ontology Validity

Метрика показывает долю отношений, соответствующих онтологии предметной области.

## Структура проекта

```text
ontology_kg_baseline/
│
├── data/
│   └── Text2KGBench/
│
├── outputs/
│
├── src/
│   ├── data_loader.py
│   ├── extractors.py
│   ├── experiment.py
│   ├── llm_client.py
│   ├── metrics.py
│   ├── ontology_validator.py
│   └── result_writer.py
│
├── run_experiment.py
├── requirements.txt
├── Dockerfile
└── README.md
```

## Настройка окружения

### Создание файла `.env`

В корневой директории проекта необходимо создать файл `.env`:

```env
CLOUDRU_API_KEY=<YOUR_API_KEY>
CLOUDRU_BASE_URL=https://foundation-models.api.cloud.ru/v1
```

## Запуск через Docker

### Сборка Docker-образа

```bash
docker build -t ontology-kg .
```

### Запуск эксперимента

```bash
docker run --env-file .env -v ${PWD}/outputs:/app/outputs ontology-kg
```

После завершения работы результаты будут сохранены в каталоге `outputs`.

## Выходные файлы

После выполнения эксперимента формируются следующие файлы:

### results_full.json

Содержит подробные результаты генерации для каждого примера набора данных.

### summary.json

Содержит усреднённые значения метрик качества по всем экспериментам.

### metrics.csv

Содержит итоговую таблицу метрик для всех исследуемых методов.

# Результаты 

После завершения эксперимента программа автоматически сохраняет результаты в папку outputs.

Основные выходные файлы:
* results_full.json — подробные результаты для каждого текста, включая извлечённые триплеты, рассчитанные метрики и результаты онтологической валидации;
* summary.json — усреднённые значения метрик для каждого исследуемого метода;
* metrics.csv — итоговая таблица с основными показателями качества;
* графики (.png) — визуальное сравнение результатов различных методов.

Итоговая таблица содержит значения следующих метрик:
* Precision	- Доля корректно извлечённых триплетов среди всех сгенерированных,
* Recall -	Доля эталонных триплетов, найденных моделью,
* F1 - Гармоническое среднее Precision и Recall,
* Fact - Precision	Точность извлечения фактов независимо от названия отношения,
* Fact Recall -	Полнота извлечения смысловых фактов,
* Fact F1 -	Интегральная оценка качества извлечения фактов,
* Ontology - Validity	Доля триплетов, полностью соответствующих онтологии.

| Метод | Precision | Recall | F1 | Fact Precision | Fact Recall | Fact F1 | Ontology Validity |
|-------|----------:|-------:|---:|---------------:|------------:|--------:|------------------:|
| Без онтологии | 0.000 | 0.000 | 0.000 | 0.933 | 0.800 | 0.853 | 0.000 |
| Отношения из онтологии | **0.600** | **0.600** | **0.600** | 0.850 | **0.867** | **0.857** | **1.000** |
| Отношения + Domain/Range | 0.567 | 0.533 | 0.547 | 0.900 | 0.800 | 0.840 | **1.000** |
| Отношения + Domain/Range + Validator | 0.567 | 0.533 | 0.547 | 0.900 | 0.800 | 0.840 | **1.000** |

<img width="875" height="398" alt="image" src="https://github.com/user-attachments/assets/42864bd8-1f1e-4c79-b6fe-b2d11ffce3ba" />

Проведённые эксперименты показывают, что использование онтологии существенно повышает качество генерации графов знаний. Наилучшие результаты достигаются при использовании списка отношений из онтологии (F1 = 0.600), а применение онтологических ограничений обеспечивает полное соответствие полученного графа онтологической модели (Ontology Validity = 1.000). При этом высокие значения Fact F1 свидетельствуют о сохранении качества извлечения фактов из текста даже при использовании дополнительных онтологических ограничений.

Такой раздел хорошо подходит для GitHub: он объясняет пользователю, какие файлы генерирует программа, что означают метрики и какой результат можно ожидать после запуска, без необходимости разбираться в коде.
